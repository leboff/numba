<li id="<%= dom_id(email) %>"> <%= email.address %>
  <%= (link_to 'x', (local_assigns.has_key? :listing_item) ? listing_item : email,  :confirm => 'Are you sure?',
               :method => :delete, :remote => true, :class => "listable_delete" ) unless (local_assigns.has_key? :isconn) ? isconn : false %></li>